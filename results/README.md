# Resultados seleccionados

Este directorio contendrá únicamente tablas y figuras finales que puedan vincularse con un script, una configuración y un conjunto de datos documentados.

Los nombres deben ser descriptivos y estables; se evitarán nombres como `final2`, `nuevo` o `corregido-final`.
